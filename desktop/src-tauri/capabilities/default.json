{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities required by the main Fine.Shyt window — minimal for C1: just the core defaults plus event listening so the splash page can react to services-ready.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
